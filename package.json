{
  "name": "sigil",
  "version": "0.1.11",
  "private": true,
  "description": "Repo root shim — the web client lives in web/. Delegates npm install/lint/test/build so tooling run from the repo root hits the real scripts in web/package.json.",
  "scripts": {
    "postinstall": "npm --prefix web install --silent",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web run test",
    "build": "npm --prefix web run build"
  }
}
